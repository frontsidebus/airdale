---
phase: 04-web-server-refactor
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - web/server.py
autonomous: false
requirements:
  - WSRV-04

must_haves:
  truths:
    - "Web server starts without errors"
    - "Status endpoint returns subsystem health"
    - "Chat WebSocket accepts messages and streams responses"
    - "Barge-in interruption cancels in-flight response and starts new one"
    - "TTS audio plays in browser during chat"
    - "Telemetry WebSocket streams sim data to browser"
    - "Transcription endpoint processes audio uploads"
  artifacts:
    - path: "web/server.py"
      provides: "Fully refactored web server"
      contains: "class AppState"
  key_links:
    - from: "web/server.py::ws_chat"
      to: "web/server.py::_stream_response"
      via: "state parameter threading"
      pattern: "_stream_response\\(ws.*state\\)"
    - from: "web/server.py::_stream_response"
      to: "web/server.py::_tts_websocket_stream"
      via: "state parameter threading"
      pattern: "_tts_websocket_stream\\(ws.*state\\)"
---

<objective>
Verify the refactored web server works correctly -- all existing functionality preserved, barge-in cancellation intact, no regressions.

Purpose: Confirm the refactor is behavior-preserving before Phase 5 builds tests against the new DI structure.
Output: Human-verified working web server.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-web-server-refactor/04-01-SUMMARY.md
@web/server.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Smoke test the refactored server</name>
  <files>web/server.py</files>
  <read_first>
    - web/server.py (verify AppState and Depends are in place)
    - .planning/phases/04-web-server-refactor/04-01-SUMMARY.md
  </read_first>
  <action>
    Run automated checks to verify the server can start and the module imports correctly:

    1. Verify the module imports without error:
       `python -c "from web.server import app, AppState, get_app_state; print('Import OK')"`

    2. Verify ruff passes:
       `ruff check web/server.py`

    3. Verify zero global statements:
       `grep -c "global " web/server.py` should return 0

    4. Verify all route handlers have Depends:
       `grep -c "Depends(get_app_state)" web/server.py` should return 5

    5. Verify AppState fields match D-01 specification:
       Check that the AppState dataclass contains: sim_client, claude_client, context_store, phase_detector, tts_client, whisper_client, tts_cache, sim_connected, bridge_last_seen, bridge_connected, settings

    6. Run existing tests if any exist:
       `cd web && python -m pytest tests/ -x 2>/dev/null || echo "No web tests yet (expected -- Phase 5 adds them)"`

    If any check fails, diagnose and fix the issue in web/server.py.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/bould/source/airdale && python -c "from web.server import app, AppState, get_app_state; print('Import OK')" && ruff check web/server.py</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from web.server import app, AppState, get_app_state"` succeeds without error
    - `ruff check web/server.py` passes
    - `grep -c "global " web/server.py` returns 0
    - `grep -c "Depends(get_app_state)" web/server.py` returns 5
  </acceptance_criteria>
  <done>Module imports cleanly, ruff passes, all structural checks confirmed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify web UI functionality after refactor</name>
  <what-built>Refactored web/server.py to use AppState dataclass with FastAPI dependency injection instead of module-level globals. All route handlers now receive state via Depends(get_app_state). Barge-in cancellation flow preserved.</what-built>
  <how-to-verify>
    1. Start the web server: `cd web && python run.py` (or `uvicorn web.server:app --port 3838`)
    2. Open http://localhost:3838 in the browser
    3. Check the status panel -- subsystem indicators should show (green/red based on what services are running)
    4. Send a text message to MERLIN via the chat input -- verify streaming text response appears
    5. If ElevenLabs is configured: verify TTS audio plays during response
    6. If MSFS is running: verify telemetry data streams in the telemetry panel
    7. Test barge-in: send a message, then while MERLIN is responding, send another message. The first response should cancel and the second should begin.
    8. Verify the /api/status endpoint returns JSON: `curl http://localhost:3838/api/status`
  </how-to-verify>
  <resume-signal>Type "approved" if all functionality works, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. Web server starts without errors
2. /api/status returns valid JSON with all subsystem fields
3. Chat WebSocket accepts and processes messages
4. Barge-in cancellation works (user confirms)
5. No runtime NameError or AttributeError from missing global references
</verification>

<success_criteria>
- Server starts and serves the web UI at http://localhost:3838
- Status endpoint returns subsystem health JSON
- Chat round-trip works (send message, receive streaming response)
- Barge-in interruption works (new message cancels in-flight response)
- No regressions in any existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/04-web-server-refactor/04-02-SUMMARY.md`
</output>
