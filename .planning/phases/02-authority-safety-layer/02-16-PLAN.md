---
phase: 02-authority-safety-layer
plan: 16
type: execute
wave: 9
depends_on: [02-11, 02-12, 02-13, 02-14, 02-15]
gap_closure: true
files_modified:
  - CLAUDE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, AUTH-06, AUTH-07, AUTH-08, CMD-07, CMD-08]

must_haves:
  truths:
    - "CLAUDE.md's test counts match what the suites actually report (IN-01)"
    - "CLAUDE.md's authority narrative describes the shipped behaviour, including the parking-brake refusal, the fuel-surface rules and the two announcement consumers"
    - "REQUIREMENTS.md records which Phase 2 requirements are satisfied, each with evidence, and no box is checked without it"
    - "The 'no new envelope rules' non-goal is reconciled in writing with the rules plan 02-14 added, rather than left as a silent contradiction"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Accurate test counts and an architectural decision that matches the code"
      contains: "drain_authority_events"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Phase 2 requirement ledger with evidence per checked item"
      contains: "AUTH-06"
  key_links:
    - from: "CLAUDE.md decision 26"
      to: "orchestrator/orchestrator/override_detector.py events"
      via: "the narrative names both consumers of the announcement queue"
      pattern: "drain_authority_events"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/02-authority-safety-layer/*-SUMMARY.md"
      via: "each checked box cites the plan and the artefact that satisfies it"
      pattern: "02-1"
---

<objective>
Make the two documents that describe this phase agree with the code it shipped.

Purpose: `CLAUDE.md` claims "~1,395 tests passing ... 1,302 orchestrator, 55 web, 38
telemetry-service" while the measured figures at verification time were 1,302 orchestrator,
92 web and 38 telemetry — and five gap-closure plans have added more since (IN-01). Its
architectural decision 26 is the authoritative narrative for the authority subsystem and
describes neither the announcement consumers, nor the parking-brake refusal, nor the
fuel-surface rules. `.planning/REQUIREMENTS.md` still has every AUTH box unchecked, including
the six the verification report found fully wired, which makes the ledger useless as a signal:
a reader cannot tell an unfinished requirement from an unrecorded one.

There is also a contradiction to settle in writing. Phase 2's stated non-goal is "no new
envelope rules", and plan 02-14 adds six. The reconciliation is real and belongs on the page
rather than in a reviewer's head: CMD-07 registered eight events in the adapter that previously
NACKed, which moved them from unreachable to live, and the rules restore the safety posture
that this phase's own change removed. They add no coverage beyond the surface this phase made
reachable.

This plan runs last because two of its three facts — the test counts and the requirement
statuses — are not knowable until every other gap-closure plan has landed.

Output: corrected counts, a decision-26 narrative that matches the code, and a requirement
ledger where every checked box carries its evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-authority-safety-layer/02-VERIFICATION.md
@.planning/phases/02-authority-safety-layer/02-REVIEW.md
@.planning/phases/02-authority-safety-layer/02-11-SUMMARY.md
@.planning/phases/02-authority-safety-layer/02-12-SUMMARY.md
@.planning/phases/02-authority-safety-layer/02-13-SUMMARY.md
@.planning/phases/02-authority-safety-layer/02-14-SUMMARY.md
@.planning/phases/02-authority-safety-layer/02-15-SUMMARY.md
@CLAUDE.md

<interfaces>
`CLAUDE.md` anchors this plan edits:
  line 340  the "~1,395 tests passing" bullet with its per-suite breakdown
  line 345  the "Test categories:" bullet
  decision 22  "Safety layers are independent of the LLM" — names `command_safety.py`,
               `command_verifier.py`, `validation.py`, `emergency.py`
  decision 26  "Authority layer -- how much MERLIN may do, and why" — the long narrative
               covering the gate, the floor, the watchdog counter, the reason field, and the
               fail-closed / fail-safe split between the CLI and the web composition roots

`.planning/REQUIREMENTS.md` anchors:
  the Phase 2 section listing AUTH-01 through AUTH-08 as unchecked
  the CMD-07 / CMD-08 / CMD-09 block
  the VARC-06 line, already checked
  the paragraph "**Explicit non-goals for Phase 2:** no new envelope rules. Those are SAFE-*
  territory and already exist."

Verification-report positions this plan must not overrule:
  AUTH-01, AUTH-03, AUTH-04, AUTH-05, AUTH-08 — SATISFIED at verification time
  AUTH-07 — SATISFIED for the "stop" half; the CLI "says so" half was WR-07, closed by 02-13
  AUTH-02 — BLOCKED by the undo false-confirmation path, closed by 02-11 and 02-12
  AUTH-06 — BLOCKED by the missing announcement, closed by 02-13 and 02-15
  CMD-07 — SATISFIED but shipped Gap 2, closed by 02-14
  CMD-08 — SATISFIED; extended to `parking_brake` by 02-14
  CMD-09 — deferred, and must stay unchecked
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bring CLAUDE.md's counts and its authority narrative in line with the code</name>
  <read_first>
    - CLAUDE.md lines 336-360 — the Testing Approach section: the counts bullet, the per-language bullets, the Test categories bullet and the "Running lint the way CI does" block.
    - CLAUDE.md decision 22 — the three-guard description (`command_safety`, `command_verifier`, `validation`, `emergency`), which the six new rules extend.
    - CLAUDE.md decision 26 — the full authority narrative, so the additions are woven into it rather than appended as a lump.
    - .planning/phases/02-authority-safety-layer/02-11-SUMMARY.md through 02-15-SUMMARY.md — what each gap-closure plan actually landed, in the executors' own words.
    - orchestrator/orchestrator/tools.py — `_was_transmitted`, `UNCONFIRMABLE_REFUSED_ACTIONS` and the reordered refusal block as shipped.
    - orchestrator/orchestrator/command_safety.py — the final `DEFAULT_RULES` list, for the count and the covered systems.
    - orchestrator/orchestrator/main.py and web/server.py — `drain_authority_events` and `_authority_event_pump`, the two names decision 26 must record.
  </read_first>
  <files>CLAUDE.md</files>
  <action>
    Measure before writing. Run each suite and record the number it prints, rather than deriving
    a figure from the plan summaries:
      `cd orchestrator && python3 -m pytest -q`
      `cd web && python3 -m pytest -q`
      `cd telemetry-service && python3 -m pytest -q`
      `cd adapters/msfs && dotnet test`
    Update the counts bullet at CLAUDE.md line 340 with the measured per-suite numbers and their
    sum. Keep the existing caveats verbatim — that xfails are reported separately, that root-level
    integration tests are deselected by default via `addopts = -m "not integration"`, and that
    they are not part of the count. If `dotnet test` cannot run in this environment, say so in the
    bullet rather than carrying a stale C# figure forward, and note it in the plan summary.

    Update the "Test categories:" bullet to name what the gap-closure work added: the transmission
    predicate and the false-confirmation regressions, the bounded announcement queue and its two
    consumers, the CLI authority status formatter, the parking-brake refusal, and the fuel /
    mixture / parking-brake safety rules. Keep every existing category.

    Extend decision 22 with one clause: the pre-execution guard now covers the fuel selector,
    mixture, crossfeed and parking brake in addition to gear, flaps, autopilot and throttle, and
    the reason it grew is that CMD-07 registered eight previously-NACKing events in the adapter —
    a rule set that does not track the reachable surface is a guard in name only.

    Extend decision 26 with the four facts it is currently missing, written into the existing
    narrative rather than bolted on:
      1. **One predicate decides whether a command was transmitted.**
         `tools.py::_was_transmitted` is `bool(result.get("success")) and "error" not in result`,
         and `web/server.py::_on_tool_result` uses the identical expression. Both halves are
         load-bearing: a negative adapter ack carries no `error` key, while the authority floor
         refusal and the ack timeout carry `success: False` *and* an `error`. Say plainly that
         every earlier attempt at this used one half or the other, and each time the pilot was
         told a command executed that had not.
      2. **The undo path pops only after the reversal is on the wire.** State the failure it
         replaces: `pop_last()` ran before the gate, so at advisory the record was destroyed and
         the result still said "Reversed GEAR_DOWN" in the past tense.
      3. **An absolute position MERLIN cannot read is refused, never toggled.**
         `UNCONFIRMABLE_POSITION_SYSTEMS` now covers `carb_heat`, `fuel_pump` and
         `parking_brake`, with `UNCONFIRMABLE_REFUSED_ACTIONS` naming the refused verbs per
         system, and the refusal runs before the unknown-control return so a refused verb gets
         the explanation rather than a dead end. Note that `parking_brake` was the only one of
         the three that was reachable, which is why it was the one that mattered.
      4. **The announcement queue has exactly one consumer per process.**
         `OverrideDetector.events` is bounded at `MAX_PENDING_ANNOUNCEMENTS`, publishing cannot
         raise inside the telemetry callback, and it is drained by
         `orchestrator.main.drain_authority_events` on the CLI and by
         `web.server._authority_event_pump` on the browser path. Record why this is called out at
         all: the queue shipped with no consumer, three separate executors noticed and each
         declined to mark AUTH-06 complete for it, and a queue behind a plausible-looking property
         accessor is exactly the shape that survives review.

    Do not restructure the file, renumber the decisions, or touch the directory listing beyond
    what these facts require.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/bould/source/airdale &amp;&amp; grep -q "drain_authority_events" CLAUDE.md &amp;&amp; grep -q "_was_transmitted" CLAUDE.md &amp;&amp; ! grep -q "~1,395 tests passing" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `CLAUDE.md` no longer contains the string `~1,395 tests passing`
    - `CLAUDE.md` no longer contains the string `55 web`
    - The counts bullet's orchestrator, web and telemetry-service figures each equal the number printed by the corresponding `pytest -q` run at execution time, and the stated total is their sum
    - `CLAUDE.md` contains `_was_transmitted`
    - `CLAUDE.md` contains `drain_authority_events` and `_authority_event_pump`
    - `CLAUDE.md` contains `UNCONFIRMABLE_REFUSED_ACTIONS`
    - `CLAUDE.md` contains `MAX_PENDING_ANNOUNCEMENTS`
    - Decision 22 names the fuel selector, mixture, crossfeed and parking brake among the systems the pre-execution guard covers
    - Decision numbering is unchanged — the last numbered architectural decision is still 26
  </acceptance_criteria>
  <done>A reader of CLAUDE.md alone gets the current test counts and an authority narrative that matches what the code does.</done>
</task>

<task type="auto">
  <name>Task 2: Record Phase 2 requirement status with evidence, and settle the envelope-rules non-goal</name>
  <read_first>
    - .planning/REQUIREMENTS.md — the whole Phase 2 section: AUTH-01 through AUTH-08, the CMD-07 / CMD-08 / CMD-09 block, the VARC-06 line, and the "Explicit non-goals for Phase 2" paragraph.
    - .planning/phases/02-authority-safety-layer/02-VERIFICATION.md — the Requirements Coverage table and the note explaining which boxes the executors declined to check and why that caution was warranted for AUTH-02 and AUTH-06 specifically.
    - .planning/phases/02-authority-safety-layer/02-11-SUMMARY.md through 02-15-SUMMARY.md — the evidence each checked box will cite.
    - .planning/ROADMAP.md Phase 2 section — the plan list, so citations name real plan files.
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    For each of AUTH-01 through AUTH-08, CMD-07 and CMD-08, decide the box from evidence and
    record that evidence inline. Do not check a box you cannot cite. The citation rule: name the
    plan that delivered it and the artefact or test that proves it, in a short parenthetical on
    the requirement line, in the style the retroactive entries elsewhere in this file already use.

    Expected outcome, to be confirmed against the code rather than assumed:
      - AUTH-01, AUTH-04, AUTH-05, AUTH-08 — satisfied at verification time; cite the original
        plans (02-01, 02-04, 02-05, 02-06, 02-08, 02-09, 02-10).
      - AUTH-03 — satisfied by 02-04, plus the absent-verdict withhold plan 02-11 added, which is
        what closes the fail-open WR-10 identified.
      - AUTH-02 — was BLOCKED on the undo false-confirmation path; cite plan 02-11 (undo pops only
        after transmission, `safety_note` gated on transmission) and plan 02-12 (the browser no
        longer renders a refused command as executed).
      - AUTH-06 — was BLOCKED on the missing announcement; cite plan 02-13 (bounded queue, CLI
        drain, spoken and printed announcement) and plan 02-15 (`authority_event` frames to every
        chat socket).
      - AUTH-07 — the "stop" half was already satisfied by 02-05 and 02-08; cite plan 02-13 for the
        CLI "says so" half (`/status` and `/authority`).
      - CMD-07 — satisfied by plan 02-02, with the safety gap it opened closed by plan 02-14. The
        citation must mention both, because CMD-07 read as complete while shipping a live hazard
        and the ledger should not lose that.
      - CMD-08 — satisfied by plan 02-04 and extended to `parking_brake` by plan 02-14.
      - CMD-09 — stays unchecked. Leave its deferral text and its sequencing constraint exactly as
        written; that constraint is what keeps an in-flight engine shutdown out of reach.
      - VARC-06 — already checked; leave it alone.
    If any of these does not hold when checked against the code and the summaries, leave that box
    unchecked and write one line saying what is still missing. An honest unchecked box is worth
    more than a checked one that a re-verification then has to contradict.

    Amend the "Explicit non-goals for Phase 2" paragraph. It currently reads "no new envelope
    rules. Those are SAFE-* territory and already exist." Keep the non-goal and add a dated
    qualification: plan 02-14 added six rules covering `FUEL_SELECTOR_OFF`, `FUEL_SELECTOR_SET`
    at index 0, `MIXTURE_SET` at idle cut-off, the three `CROSS_FEED_*` events and
    `PARKING_BRAKES`, and this does not breach the non-goal because it adds no coverage beyond
    the surface this phase itself made reachable. CMD-07 registered eight previously-NACKing
    events in the adapter's `CommandMap`, turning a refusal into a real `TransmitClientEvent`;
    the rules restore the posture that change removed, and `MAGNETO_SET` was held back from the
    identical treatment for the identical reason. State explicitly that a phase which widens the
    write surface owns the rules for what it widened, and that a broader `SAFE-*` envelope pass
    for the systems that remain unruled is still outstanding.

    Do not renumber requirements, do not add new requirement IDs, and do not touch any section
    outside Phase 2 and the non-goals paragraph.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/bould/source/airdale &amp;&amp; grep -q "\[ \] \*\*CMD-09\*\*" .planning/REQUIREMENTS.md &amp;&amp; grep -q "\[x\] \*\*AUTH-06\*\*" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` shows `- [x] **AUTH-02**` and `- [x] **AUTH-06**`, each with a parenthetical naming the gap-closure plan that unblocked it
    - Every checked AUTH or CMD box in the Phase 2 section carries a citation naming at least one plan file
    - `CMD-09` remains `- [ ]` and its sequencing-constraint sentence is byte-identical to before this task
    - `VARC-06` remains checked and its text is unchanged
    - The non-goals paragraph still states "no new envelope rules" and now also explains the six rules plan 02-14 added and why they do not breach it
    - The non-goals paragraph names `CMD-07` as the reason the reachable surface widened
    - No requirement ID was added, removed or renumbered: `grep -c "^\- \[.\] \*\*" .planning/REQUIREMENTS.md` reports the same value as before this task
  </acceptance_criteria>
  <done>The requirement ledger says which Phase 2 requirements are met and shows its working, and the one apparent contradiction between the phase's non-goal and its code is explained rather than hidden.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation -> future implementer | `CLAUDE.md` is loaded as project instructions by every agent that touches this repository; a wrong statement there propagates into code |
| requirement ledger -> verification | A checked box is an assertion a verifier will trust unless it re-derives the evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-16-01 | Spoofing | A checked requirement box that is not actually satisfied | mitigate | Every checked box must carry a plan-and-artefact citation, and the task instructs leaving a box unchecked with a stated reason rather than guessing. Severity high — this is the ledger the next phase plans against |
| T-02-16-02 | Spoofing | Test counts asserted rather than measured | mitigate | The action requires running each suite and recording its printed figure; the acceptance criteria tie the numbers to the run at execution time. Severity low operationally, but the stale count is precisely the defect IN-01 raised |
| T-02-16-03 | Repudiation | The envelope-rules non-goal silently contradicted by shipped code | mitigate | The contradiction is written down and reconciled in the non-goals paragraph, naming CMD-07 as the cause and `MAGNETO_SET` as the precedent. Severity medium — an unexplained contradiction invites a future contributor to "fix" it by deleting the rules |
| T-02-16-04 | Tampering | An edit that accidentally re-enables the CMD-09 deferral | mitigate | The acceptance criteria require `CMD-09` to remain unchecked with its sequencing sentence byte-identical. Severity high if violated — that sentence is what keeps a working in-flight engine shutdown out of the adapter |
| T-02-16-05 | Elevation of Privilege | A documentation change that alters executable behaviour | mitigate | This plan touches two Markdown files and no source, and no acceptance criterion asks for a code edit. Severity low |
| T-02-16-SC | Tampering | pip / npm installs | accept | This plan installs no packages and runs the existing suites only, to measure them. `02-RESEARCH.md` § Package Legitimacy Audit records zero new dependencies |
</threat_model>

<verification>
- `cd orchestrator && python3 -m pytest -q` exits 0 and its count matches the figure written into CLAUDE.md
- `cd web && python3 -m pytest -q` exits 0 and its count matches the figure written into CLAUDE.md
- `cd telemetry-service && python3 -m pytest -q` exits 0 and its count matches the figure written into CLAUDE.md
- `ruff check orchestrator/ telemetry-service/ web/ --config orchestrator/pyproject.toml --extend-ignore SIM105,SIM117,F841,B008,B017,B007,UP041` exits 0 from the repo root
- `git diff --name-only` after this plan lists only `CLAUDE.md` and `.planning/REQUIREMENTS.md`
- `grep -rn "MAGNETO_SET\|TOGGLE_STARTER1" adapters/msfs/SimConnectManager.cs` still produces no output

</verification>

<success_criteria>
- CLAUDE.md's test counts are measured, not inherited
- CLAUDE.md decision 26 names the transmission predicate, the undo ordering, the three-system refusal table and both announcement consumers
- Every Phase 2 requirement box reflects the code, with a citation
- CMD-09 stays deferred with its sequencing constraint intact
- The envelope-rules non-goal and the six new rules are reconciled in one paragraph a future reader can act on
</success_criteria>

<notes>
**Warning-level findings folded in:** IN-01 (stale test counts) — explicitly named in the gap
brief as a candidate, and this is the only plan that can measure the final figures.

**Deliberately deferred, with reasons:**
- Updating `.planning/STATE.md`'s "Requirement coverage: 42 of 63 (67%)" line and the Phase 2
  progress block — STATE.md is maintained by the phase orchestrator on completion, and a plan
  writing to it would race with that.
- Marking the ROADMAP Phase 2 plan checkboxes for 02-11 through 02-16 — same reason; the
  orchestrator owns the roadmap's execution ledger.
- Re-running verification and updating `02-VERIFICATION.md` — a plan must not grade its own
  phase. The re-verification pass is a separate step and this plan deliberately leaves the
  verification report untouched so the before-and-after remains legible.
</notes>

<output>
Create `.planning/phases/02-authority-safety-layer/02-16-SUMMARY.md` when done
</output>
