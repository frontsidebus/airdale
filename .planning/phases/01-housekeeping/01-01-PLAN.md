---
phase: 01-housekeeping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - orchestrator/orchestrator/config.py
  - orchestrator/orchestrator/sim_client.py
  - orchestrator/tests/test_config.py
  - orchestrator/tests/conftest.py
  - tests/integration/test_orchestrator_e2e.py
  - docker-compose.yml
  - .env.example
  - docs/API.md
  - docs/ARCHITECTURE.md
autonomous: true
requirements: [HSKP-01, HSKP-07]

must_haves:
  truths:
    - "No Python file references simconnect_ws_host, simconnect_ws_port, simconnect_bridge_url, or SimConnectClient"
    - "No YAML or env file references SIMCONNECT_BRIDGE_URL"
    - "No documentation references SimConnectClient as a current class name"
    - "All existing tests pass after removal"
  artifacts:
    - path: "orchestrator/orchestrator/config.py"
      provides: "Settings class without deprecated fields"
      contains: "_build_derived"
    - path: "orchestrator/orchestrator/sim_client.py"
      provides: "TelemetryClient without backward-compat alias"
  key_links:
    - from: "orchestrator/orchestrator/config.py"
      to: "orchestrator/tests/test_config.py"
      via: "Settings class instantiation"
      pattern: "Settings\\("
    - from: "docker-compose.yml"
      to: ".env"
      via: "environment variables"
      pattern: "SIMCONNECT_BRIDGE_URL"
---

<objective>
Remove all deprecated SimConnect config fields, the backward-compat alias, and references across the entire codebase. Also remove the deprecated SIMCONNECT_BRIDGE_URL env var from docker-compose.yml.

Purpose: Eliminate dead config weight from the legacy direct-bridge architecture that was replaced by the universal telemetry service. Prevents confusion and test fragility.
Output: Clean Settings class, clean docker-compose.yml, passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-housekeeping/01-CONTEXT.md
@.planning/phases/01-housekeeping/01-RESEARCH.md

@orchestrator/orchestrator/config.py
@orchestrator/orchestrator/sim_client.py
@orchestrator/tests/test_config.py
@orchestrator/tests/conftest.py
@tests/integration/test_orchestrator_e2e.py
@docker-compose.yml
@.env.example
@docs/API.md
@docs/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove deprecated SimConnect config fields and alias from Python code</name>
  <files>
    orchestrator/orchestrator/config.py,
    orchestrator/orchestrator/sim_client.py,
    orchestrator/tests/test_config.py,
    orchestrator/tests/conftest.py,
    tests/integration/test_orchestrator_e2e.py
  </files>
  <read_first>
    orchestrator/orchestrator/config.py,
    orchestrator/orchestrator/sim_client.py,
    orchestrator/tests/test_config.py,
    orchestrator/tests/conftest.py,
    tests/integration/test_orchestrator_e2e.py
  </read_first>
  <action>
Per D-01 and D-02:

1. **orchestrator/orchestrator/config.py:**
   - Delete lines 61-73 (the entire `# --- Legacy SimConnect bridge` section with fields `simconnect_ws_host`, `simconnect_ws_port`, `simconnect_bridge_url`)
   - Delete lines 126-129 from the `_build_derived` validator (the `if not self.simconnect_bridge_url:` block that constructs the legacy URL). Keep the validator method -- it still handles `telemetry_service_url` construction. The final validator should be:
     ```python
     @model_validator(mode="after")
     def _build_derived(self) -> Settings:
         if not self.telemetry_service_url:
             self.telemetry_service_url = (
                 f"ws://{self.telemetry_service_host}"
                 f":{self.telemetry_service_port}/ws/telemetry"
             )
         return self
     ```

2. **orchestrator/orchestrator/sim_client.py:**
   - Delete the line `SimConnectClient = TelemetryClient` (line 526, the backward-compat alias at the end of the file)

3. **orchestrator/tests/test_config.py:**
   - Delete the `test_default_simconnect_url` test method (lines 15-18)
   - Delete the `test_env_overrides_simconnect_url` test method (lines 69-71)

4. **orchestrator/tests/conftest.py:**
   - Delete the `"SIMCONNECT_BRIDGE_URL"` entry from the `mock_env_vars` dict (line 49). Remove the entire key-value pair.

5. **tests/integration/test_orchestrator_e2e.py:**
   - Remove the `simconnect_bridge_url=` keyword argument from the `Settings()` call on line 40. The Settings class has `extra = "ignore"` but explicit keyword arguments for removed fields will cause a TypeError.

After all edits, run: `grep -rn "simconnect_ws_host\|simconnect_ws_port\|simconnect_bridge_url\|SimConnectClient" --include="*.py" orchestrator/ tests/` to confirm zero matches.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/bould/source/airdale && grep -rn "simconnect_ws_host\|simconnect_ws_port\|simconnect_bridge_url\|SimConnectClient" --include="*.py" orchestrator/ tests/ ; echo "EXIT:$?" && cd orchestrator && ruff check . && ruff format --check .</automated>
  </verify>
  <acceptance_criteria>
    - config.py does NOT contain the string "simconnect_ws_host"
    - config.py does NOT contain the string "simconnect_bridge_url"
    - config.py contains `def _build_derived` (validator preserved)
    - config.py contains `telemetry_service_url` in the validator (telemetry logic preserved)
    - sim_client.py does NOT contain "SimConnectClient"
    - test_config.py does NOT contain "test_default_simconnect_url"
    - test_config.py does NOT contain "test_env_overrides_simconnect_url"
    - conftest.py does NOT contain "SIMCONNECT_BRIDGE_URL"
    - test_orchestrator_e2e.py does NOT contain "simconnect_bridge_url="
    - `grep -rn simconnect --include="*.py" orchestrator/ tests/` returns no matches
    - `ruff check .` and `ruff format --check .` pass in orchestrator/
  </acceptance_criteria>
  <done>All deprecated SimConnect Python references removed. Validator preserved with only telemetry URL logic. No Python file contains simconnect_ws_host, simconnect_ws_port, simconnect_bridge_url, or SimConnectClient. Ruff lint and format checks pass.</done>
</task>

<task type="auto">
  <name>Task 2: Remove deprecated references from config files and documentation</name>
  <files>
    docker-compose.yml,
    .env.example,
    docs/API.md,
    docs/ARCHITECTURE.md
  </files>
  <read_first>
    docker-compose.yml,
    .env.example,
    docs/API.md,
    docs/ARCHITECTURE.md
  </read_first>
  <action>
Per D-01 and HSKP-07:

1. **docker-compose.yml:**
   - Delete line 126-127 (the comment `# Legacy fallback (still works via telemetry service)` and the `- SIMCONNECT_BRIDGE_URL=ws://telemetry-service:8080/ws/telemetry` environment variable line from the orchestrator service)

2. **.env.example:**
   - Delete the commented-out legacy SimConnect settings block (around lines 50-53). This typically includes lines with `SIMCONNECT_WS_HOST`, `SIMCONNECT_WS_PORT`, `SIMCONNECT_BRIDGE_URL`. Remove the entire block including any section header comment.

3. **docs/API.md:**
   - Find the SimConnect Bridge env var table section (around line 533-541). Remove the entire row/section referencing `SIMCONNECT_BRIDGE_URL`. If it's a table row in an environment variables table, just remove the row. If it's a standalone section, remove the section.

4. **docs/ARCHITECTURE.md:**
   - At line 116 and line 260, replace `SimConnectClient` with `TelemetryClient` in the narrative text. These are documentation references to the class name.

After all edits, run: `grep -rn "SIMCONNECT_BRIDGE_URL\|SimConnectClient\|simconnect_ws_host\|simconnect_ws_port\|simconnect_bridge_url" --include="*.yml" --include="*.md" --include="*.env*" .` to confirm zero matches (excluding CONTEXT.md, RESEARCH.md, and PLAN files).
  </action>
  <verify>
    <automated>cd /mnt/c/Users/bould/source/airdale && grep -rn "SIMCONNECT_BRIDGE_URL\|SimConnectClient" docker-compose.yml .env.example docs/API.md docs/ARCHITECTURE.md ; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.yml does NOT contain "SIMCONNECT_BRIDGE_URL"
    - .env.example does NOT contain "SIMCONNECT_BRIDGE_URL"
    - .env.example does NOT contain "SIMCONNECT_WS_HOST"
    - docs/API.md does NOT contain "SIMCONNECT_BRIDGE_URL"
    - docs/ARCHITECTURE.md does NOT contain "SimConnectClient"
    - docs/ARCHITECTURE.md contains "TelemetryClient" (replacement text)
  </acceptance_criteria>
  <done>All deprecated SimConnect references removed from docker-compose.yml, .env.example, and documentation. docs/ARCHITECTURE.md updated to use TelemetryClient.</done>
</task>

</tasks>

<verification>
1. `grep -rn "simconnect" --include="*.py" --include="*.yml" --include="*.md" --include="*.env*" . | grep -v ".planning/" | grep -v "adapters/"` returns no matches (adapters/ excluded because the C# MSFS adapter legitimately uses SimConnect)
2. `cd orchestrator && python -m pytest tests/ -x --tb=short` passes
3. `cd telemetry-service && python -m pytest tests/ -x --tb=short` passes
</verification>

<success_criteria>
- Zero references to deprecated SimConnect config in Python code, YAML, docs, or env files
- All orchestrator tests pass
- docker-compose.yml orchestrator environment block has no SIMCONNECT_BRIDGE_URL line
</success_criteria>

<output>
After completion, create `.planning/phases/01-housekeeping/01-01-SUMMARY.md`
</output>
