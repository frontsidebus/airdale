---
phase: 04-web-server-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/server.py
autonomous: true
requirements:
  - WSRV-01
  - WSRV-02
  - WSRV-03

must_haves:
  truths:
    - "Zero module-level mutable variables in web/server.py"
    - "All route handlers receive AppState via Depends(get_app_state)"
    - "All helper functions receive AppState as an explicit parameter"
    - "Lifespan creates AppState and assigns to app.state.app_state"
    - "No 'global' statements remain in the file"
    - "Barge-in cancellation flow is structurally identical (closures unchanged)"
  artifacts:
    - path: "web/server.py"
      provides: "Web server with DI-based state management"
      contains: "class AppState"
    - path: "web/server.py"
      provides: "Dependency callable"
      contains: "def get_app_state"
  key_links:
    - from: "web/server.py::lifespan"
      to: "app.state.app_state"
      via: "AppState instance creation and assignment"
      pattern: "app\\.state\\.app_state\\s*=\\s*state"
    - from: "web/server.py::get_app_state"
      to: "request.app.state.app_state"
      via: "FastAPI Depends injection"
      pattern: "request\\.app\\.state\\.app_state"
    - from: "web/server.py::route handlers"
      to: "get_app_state"
      via: "Depends(get_app_state) in function signature"
      pattern: "Depends\\(get_app_state\\)"
---

<objective>
Refactor web/server.py to replace all module-level mutable state with a typed AppState dataclass on app.state, accessed via FastAPI Depends(). Preserve identical runtime behavior including barge-in cancellation.

Purpose: Make the web server testable via dependency_overrides (Phase 5 depends on this). Eliminate global mutation.
Output: Refactored web/server.py with zero module-level mutable state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-web-server-refactor/04-CONTEXT.md
@.planning/phases/04-web-server-refactor/04-RESEARCH.md
@web/server.py

<interfaces>
<!-- Key types the executor needs from the existing codebase -->

From orchestrator/orchestrator/whisper_client.py:
```python
class WhisperClient:
    async def transcribe_with_confidence(self, audio_bytes: bytes) -> TranscriptionResult: ...
    async def is_available(self) -> bool: ...
    async def aclose(self) -> None: ...
```

From orchestrator/orchestrator/tts/base.py:
```python
class TTSClient(Protocol):
    async def synthesize(self, text: str) -> bytes: ...
    async def aclose(self) -> None: ...
```

From orchestrator/orchestrator/claude_client.py:
```python
class ClaudeClient:
    async def chat(self, user_text: str, sim_state: SimState | None = None) -> AsyncGenerator[str, None]: ...
```

From orchestrator/orchestrator/sim_client.py:
```python
class TelemetryClient:
    async def connect(self) -> None: ...
    async def disconnect(self) -> None: ...
    def subscribe(self, callback: Callable) -> None: ...
    state: SimState
```

From orchestrator/orchestrator/config.py:
```python
def load_settings() -> Settings: ...
```

From orchestrator/orchestrator/context_store.py:
```python
class ContextStore:
    available: bool
    document_count: int
```

From orchestrator/orchestrator/flight_phase.py:
```python
class FlightPhaseDetector:
    def update(self, state: SimState) -> FlightPhase: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define AppState dataclass, get_app_state dependency, and refactor lifespan</name>
  <files>web/server.py</files>
  <read_first>
    - web/server.py (full file -- focus on lines 57-100 for globals, lines 156-234 for lifespan)
    - .planning/phases/04-web-server-refactor/04-RESEARCH.md (Global Reference Map section)
    - .planning/phases/04-web-server-refactor/04-CONTEXT.md (Decisions D-01 through D-12)
  </read_first>
  <action>
    **Step 1: Add AppState dataclass** (per D-01)

    After the imports and before the logging section (~line 40), add:

    ```python
    from dataclasses import dataclass, field
    from fastapi import Depends, Request
    ```

    Add the `Depends` and `Request` imports to the existing FastAPI import line (they are not currently imported).

    After the constants section (after `_CACHEABLE_PHRASES` list, ~line 112), define:

    ```python
    @dataclass
    class AppState:
        """Mutable shared state for the MERLIN web server."""
        settings: Any  # Settings type from orchestrator.config
        sim_client: TelemetryClient | None = None
        claude_client: ClaudeClient | None = None
        context_store: ContextStore | None = None
        phase_detector: FlightPhaseDetector | None = None
        whisper_client: WhisperClient | None = None
        tts_client: httpx.AsyncClient | None = None
        tts_cache: dict[str, bytes] = field(default_factory=dict)
        sim_connected: bool = False
        bridge_last_seen: float = 0.0
        bridge_connected: bool = False
    ```

    Include `settings` in AppState for testability (Phase 5 can override it). Keep the module-level `settings = load_settings()` for logging setup that runs before lifespan.

    **Step 2: Add get_app_state dependency callable** (per D-02, D-06)

    Right after AppState, add:

    ```python
    def get_app_state(request: Request) -> AppState:
        """FastAPI dependency: extract AppState from app.state."""
        return request.app.state.app_state
    ```

    **Step 3: Remove module-level mutable globals** (per D-12)

    DELETE these lines entirely (currently lines 57-67, 81, 92, 99):
    - `sim_client: TelemetryClient | None = None`
    - `claude_client: ClaudeClient | None = None`
    - `context_store: ContextStore | None = None`
    - `phase_detector: FlightPhaseDetector | None = None`
    - `_sim_connected: bool = False`
    - `_bridge_last_seen: float = 0.0`
    - `_bridge_connected: bool = False`
    - `_tts_client: httpx.AsyncClient | None = None`
    - `_whisper_client: WhisperClient | None = None`
    - `_TTS_CACHE: dict[str, bytes] = {}`

    DELETE the `_get_tts_client()` function entirely (lines 84-88).
    DELETE the `_get_whisper_client()` function if present.

    Keep these module-level (per D-03, they are immutable):
    - `settings = load_settings()` and the logging setup
    - `_LOW_CONFIDENCE_THRESHOLD = 0.4`
    - `_POST_SPEECH_PAUSE_SECS = 0.3`
    - `_CACHEABLE_PHRASES = [...]`
    - `_STATIC_DIR = Path(...)`

    **Step 4: Refactor lifespan** (per D-10, D-11)

    Replace the lifespan function. Remove `global` statements (lines 158-159). Create AppState instance, populate fields, assign to `app.state.app_state`.

    The new lifespan should:
    1. Create `state = AppState(settings=settings)`
    2. Create ContextStore and assign to `state.context_store`
    3. Create TelemetryClient, connect, assign to `state.sim_client`, set `state.sim_connected`
    4. Create FlightPhaseDetector, assign to `state.phase_detector`
    5. Register `_on_state` callback -- the callback captures `state` from lifespan scope (replace `assert phase_detector is not None` with `assert state.phase_detector is not None`; use `state.phase_detector.update(s)`)
    6. Create WhisperClient, assign to `state.whisper_client`
    7. Create ClaudeClient, assign to `state.claude_client`
    8. Create httpx.AsyncClient(timeout=30.0), assign to `state.tts_client`
    9. Call `_prepopulate_tts_cache(state)` (pass state as parameter)
    10. `app.state.app_state = state`
    11. `yield`
    12. Cleanup: disconnect sim_client if connected, aclose tts_client if not closed, aclose whisper_client if not None

    **Step 5: Update _prepopulate_tts_cache** (per D-05)

    Change signature from `async def _prepopulate_tts_cache() -> None` to `async def _prepopulate_tts_cache(state: AppState) -> None`.

    Replace:
    - `settings.elevenlabs_api_key` -> `state.settings.elevenlabs_api_key`
    - `settings.voice_id` -> `state.settings.voice_id`
    - `client = await _get_tts_client()` -> `client = state.tts_client` (with a None check)
    - `_TTS_CACHE[sanitized] = resp.content` -> `state.tts_cache[sanitized] = resp.content`
    - `sanitized in _TTS_CACHE` -> `sanitized in state.tts_cache`
    - `settings.elevenlabs_model_id` -> `state.settings.elevenlabs_model_id`

    Do NOT change the lifespan call site yet -- it should now be `_prepopulate_tts_cache(state)`.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/bould/source/airdale && python -c "import ast; ast.parse(open('web/server.py').read()); print('Syntax OK')" && ruff check web/server.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^class AppState" web/server.py` returns 1
    - `grep -c "def get_app_state" web/server.py` returns 1
    - `grep -c "app.state.app_state = state" web/server.py` returns 1
    - `grep -c "^sim_client:" web/server.py` returns 0 (module-level global removed)
    - `grep -c "^claude_client:" web/server.py` returns 0
    - `grep -c "^_sim_connected:" web/server.py` returns 0
    - `grep -c "^_bridge_last_seen:" web/server.py` returns 0
    - `grep -c "^_bridge_connected:" web/server.py` returns 0
    - `grep -c "^_tts_client:" web/server.py` returns 0
    - `grep -c "^_whisper_client:" web/server.py` returns 0
    - `grep -c "^_TTS_CACHE:" web/server.py` returns 0
    - `grep -c "def _get_tts_client" web/server.py` returns 0
    - `grep -c "global " web/server.py` returns 0
    - `grep "request.app.state.app_state" web/server.py` matches
    - `ruff check web/server.py` passes (or only pre-existing warnings)
  </acceptance_criteria>
  <done>AppState dataclass defined with all 11 fields. get_app_state dependency callable defined. Lifespan creates AppState, populates all fields, assigns to app.state.app_state. All module-level mutable globals removed. _get_tts_client() function removed. _prepopulate_tts_cache takes state parameter. No 'global' statements remain. File parses without syntax errors.</done>
</task>

<task type="auto">
  <name>Task 2: Wire all route handlers and helpers to use AppState via Depends and parameter passing</name>
  <files>web/server.py</files>
  <read_first>
    - web/server.py (re-read after Task 1 modifications)
    - .planning/phases/04-web-server-refactor/04-RESEARCH.md (Function Signature Changes Summary and Barge-in Flow sections)
  </read_first>
  <action>
    Update every route handler to accept `state: AppState = Depends(get_app_state)` and every helper to accept `state: AppState` as a parameter. Replace all former global references with `state.X`.

    **Route handler updates (per D-04):**

    1. **get_status()** -> `get_status(state: AppState = Depends(get_app_state))`:
       - `_whisper_client` -> `state.whisper_client`
       - `context_store` -> `state.context_store`
       - `_bridge_connected` -> `state.bridge_connected`
       - `_bridge_last_seen` -> `state.bridge_last_seen`
       - `_sim_connected` -> `state.sim_connected`
       - `settings` -> `state.settings` (for `elevenlabs_api_key`, `voice_id`, `claude_model`, `telemetry_service_url`)

    2. **transcribe_audio(file)** -> `transcribe_audio(file: UploadFile, state: AppState = Depends(get_app_state))`:
       - Pass `state` to `_transcribe_with_confidence(..., state=state)` and `_transcribe_audio_bytes_with_confidence(..., state=state)`

    3. **text_to_speech(request)** -> `text_to_speech(request: TTSRequest, state: AppState = Depends(get_app_state))`:
       - `settings` -> `state.settings`
       - `_TTS_CACHE` -> `state.tts_cache`
       - `_get_tts_client()` -> `state.tts_client` (with None check: `if state.tts_client is None: return error`)
       - Remove `client = await _get_tts_client()`, use `state.tts_client` directly

    4. **ws_telemetry(ws)** -> `ws_telemetry(ws: WebSocket, state: AppState = Depends(get_app_state))`:
       - Remove `global _bridge_last_seen, _bridge_connected` statement
       - `settings.telemetry_service_url` -> `state.settings.telemetry_service_url`
       - `_bridge_last_seen = time.monotonic()` -> `state.bridge_last_seen = time.monotonic()`
       - `_bridge_connected = sim_active` -> `state.bridge_connected = sim_active`
       - `phase_detector` -> `state.phase_detector`

    5. **ws_chat(ws)** -> `ws_chat(ws: WebSocket, state: AppState = Depends(get_app_state))`:
       - Pass `state` to `_stream_response(ws, user_text, interrupt_event, state)`
       - Pass `state` to `_transcribe_audio_bytes_with_confidence(audio_bytes, mime, state)`
       - The `_cancel_active_response()` closure references only local variables (`active_response_task`, `interrupt_event`, `ws`) -- per D-07, do NOT restructure it. No changes needed to the closure itself.

    **Helper function updates (per D-05, D-08):**

    6. **_stream_response(ws, user_text, interrupt)** -> add `state: AppState` parameter:
       - `settings` -> `state.settings`
       - `_get_tts_client()` -> `state.tts_client` (in _warmup_tts closure)
       - `claude_client` -> `state.claude_client`
       - `_sim_connected` -> `state.sim_connected`
       - `sim_client` -> `state.sim_client`
       - `phase_detector` -> `state.phase_detector`
       - Pass `state` to `_tts_websocket_stream(ws, tts_queue, interrupt, state)`

    7. **_tts_websocket_stream(ws, tts_queue, interrupt)** -> add `state: AppState` parameter:
       - `settings.voice_id` -> `state.settings.voice_id`
       - `settings.elevenlabs_model_id` -> `state.settings.elevenlabs_model_id`
       - `settings.elevenlabs_api_key` -> `state.settings.elevenlabs_api_key`
       - `_TTS_CACHE` -> `state.tts_cache`
       - Pass `state` to `_tts_rest_fallback(ws, tts_queue, interrupt, state)`

    8. **_tts_rest_fallback(ws, tts_queue, interrupt)** -> add `state: AppState` parameter:
       - Pass `state` to `_send_tts_chunk_rest(ws, sentence, state)`

    9. **_send_tts_chunk_rest(ws, text)** -> add `state: AppState` parameter:
       - `_TTS_CACHE` -> `state.tts_cache`
       - `_get_tts_client()` -> `state.tts_client`
       - `settings` -> `state.settings`
       - Remove `client = await _get_tts_client()`, use `state.tts_client` directly

    10. **_transcribe_with_confidence(audio_bytes, ...)** -> add `state: AppState` parameter:
        - `_whisper_client` -> `state.whisper_client`

    11. **_transcribe_audio_bytes_with_confidence(audio_bytes, mime_type)** -> add `state: AppState` parameter:
        - Pass `state` to `_transcribe_with_confidence(..., state=state)` calls

    **Functions unchanged (per research):**
    - `_split_at_sentence(text)` -- pure function, no state
    - `index()` -- serves static file, no mutable state

    **Final sweep:** After all changes, search the entire file for any remaining references to the old global names (`sim_client`, `claude_client`, `context_store`, `phase_detector`, `_sim_connected`, `_bridge_last_seen`, `_bridge_connected`, `_tts_client_instance`, `_whisper_client`, `_TTS_CACHE`, `_get_tts_client`) that are NOT qualified with `state.` or inside the AppState class definition. Fix any found.

    Run `ruff check web/server.py` and `ruff format web/server.py` to fix any formatting issues.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/bould/source/airdale && python -c "import ast; ast.parse(open('web/server.py').read()); print('Syntax OK')" && ruff check web/server.py && python -c "
# Verify no remaining bare global references (outside class def and imports)
import re
with open('web/server.py') as f:
    content = f.read()
# Check no 'global ' statements
assert 'global ' not in content, 'Found global statement'
# Check Depends appears in route handlers
assert content.count('Depends(get_app_state)') >= 5, f'Expected 5+ Depends, found {content.count(\"Depends(get_app_state)\")}'
print('All checks passed')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Depends(get_app_state)" web/server.py` returns 5 (get_status, transcribe_audio, text_to_speech, ws_telemetry, ws_chat)
    - `grep -c "global " web/server.py` returns 0
    - `grep "def get_status" web/server.py` shows `state: AppState = Depends(get_app_state)` in signature
    - `grep "def transcribe_audio" web/server.py` shows `state: AppState = Depends(get_app_state)` in signature
    - `grep "def text_to_speech" web/server.py` shows `state: AppState = Depends(get_app_state)` in signature
    - `grep "def ws_telemetry" web/server.py` shows `state: AppState = Depends(get_app_state)` in signature
    - `grep "def ws_chat" web/server.py` shows `state: AppState = Depends(get_app_state)` in signature
    - `grep "def _stream_response" web/server.py` shows `state: AppState` in signature
    - `grep "def _tts_websocket_stream" web/server.py` shows `state: AppState` in signature
    - `grep "def _transcribe_with_confidence" web/server.py` shows `state: AppState` in signature
    - `ruff check web/server.py` passes
    - `python -c "import ast; ast.parse(open('web/server.py').read())"` succeeds
  </acceptance_criteria>
  <done>All 5 route handlers use Depends(get_app_state). All 7 helper functions accept state: AppState parameter. Zero global statements remain. All former global references replaced with state.X access. ruff check passes. File parses without errors.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `python -c "import ast; ast.parse(open('web/server.py').read())"` -- file parses
2. `ruff check web/server.py` -- linter passes
3. `grep -c "global " web/server.py` returns 0 -- no global statements
4. `grep -c "Depends(get_app_state)" web/server.py` returns 5 -- all route handlers injected
5. `grep -c "class AppState" web/server.py` returns 1 -- dataclass exists
6. `grep -c "app.state.app_state" web/server.py` returns at least 2 -- lifespan assignment + dependency getter
</verification>

<success_criteria>
- web/server.py has zero module-level mutable state
- AppState dataclass contains all 11 previously-global fields
- Lifespan creates AppState and assigns to app.state.app_state
- All 5 route handlers use Depends(get_app_state)
- All helper functions receive state as explicit parameter
- No 'global' statements anywhere in the file
- Barge-in closure structure unchanged (only _stream_response call site updated)
- ruff check passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-web-server-refactor/04-01-SUMMARY.md`
</output>
