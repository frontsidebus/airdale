---
phase: 01-housekeeping
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - docker-compose.yml
  - telemetry-service/Dockerfile
autonomous: true
requirements: [HSKP-03, HSKP-04, HSKP-05]

must_haves:
  truths:
    - "ChromaDB Docker image uses a pinned version tag, not :latest"
    - "Whisper Docker image uses a pinned version tag, not :latest-cpu or :latest-cuda"
    - "All Dockerfiles use the same Python base image version"
    - "docker compose config renders valid YAML with pinned tags"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Service definitions with pinned image tags"
      contains: "chromadb/chroma:"
    - path: "telemetry-service/Dockerfile"
      provides: "Telemetry service build on python:3.12-slim"
      contains: "python:3.12-slim"
  key_links:
    - from: "docker-compose.yml"
      to: "Docker Hub"
      via: "image pull"
      pattern: "image:"
---

<objective>
Pin all third-party Docker images to specific version tags and standardize Python version across Dockerfiles.

Purpose: Ensure reproducible builds. Prevent surprise breakage from upstream image updates.
Output: docker-compose.yml with pinned tags, telemetry-service Dockerfile on python:3.12-slim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-housekeeping/01-CONTEXT.md
@.planning/phases/01-housekeeping/01-RESEARCH.md

@docker-compose.yml
@telemetry-service/Dockerfile
@orchestrator/Dockerfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin Docker image versions and standardize Python</name>
  <files>
    docker-compose.yml,
    telemetry-service/Dockerfile
  </files>
  <read_first>
    docker-compose.yml,
    telemetry-service/Dockerfile,
    orchestrator/Dockerfile
  </read_first>
  <action>
Per D-03, D-04, D-05:

1. **docker-compose.yml -- Pin ChromaDB image (HSKP-03):**
   - Change line 59 from `image: chromadb/chroma:latest` to `image: chromadb/chroma:1.5.5`
   - Note: Per D-04, use minor-range tags. However, if `1.5` shorthand does not exist on Docker Hub, use the full patch version `1.5.5`. Before committing, verify the tag exists by running: `docker manifest inspect chromadb/chroma:1.5 2>/dev/null || echo "Use 1.5.5 instead"`
   - Try `1.5` first. If that fails, use `1.5.5`.

2. **docker-compose.yml -- Pin Whisper CPU image (HSKP-04):**
   - Change line 21 from `image: fedirz/faster-whisper-server:latest-cpu` to `image: fedirz/faster-whisper-server:0.8-cpu`
   - Verify tag exists: `docker manifest inspect fedirz/faster-whisper-server:0.8-cpu 2>/dev/null || echo "Use 0.8.3-cpu instead"`
   - If `0.8-cpu` does not exist, use `0.8.3-cpu`.

3. **docker-compose.yml -- Pin Whisper GPU image (D-05, HSKP-04):**
   - Change the commented-out GPU line (line 45) from `# image: fedirz/faster-whisper-server:latest-cuda` to `# image: fedirz/faster-whisper-server:0.8-cuda`
   - Same version logic: if `0.8-cuda` doesn't exist, use `0.8.3-cuda`.

4. **telemetry-service/Dockerfile -- Standardize Python (HSKP-05):**
   - Change line 1 from `FROM python:3.11-slim` to `FROM python:3.12-slim`
   - This matches the orchestrator Dockerfile which already uses `python:3.12-slim`.

After edits, run `docker compose config` to verify the YAML is valid.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/bould/source/airdale && grep -n "image:" docker-compose.yml && grep -n "FROM" telemetry-service/Dockerfile orchestrator/Dockerfile</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.yml does NOT contain "chromadb/chroma:latest"
    - docker-compose.yml contains "chromadb/chroma:1.5" (either `1.5` or `1.5.5`)
    - docker-compose.yml does NOT contain "faster-whisper-server:latest-cpu"
    - docker-compose.yml contains "faster-whisper-server:0.8" (either `0.8-cpu` or `0.8.3-cpu`)
    - docker-compose.yml commented GPU line contains "0.8" (either `0.8-cuda` or `0.8.3-cuda`), NOT "latest-cuda"
    - telemetry-service/Dockerfile line 1 is `FROM python:3.12-slim`
    - orchestrator/Dockerfile contains `FROM python:3.12-slim` (unchanged, confirms match)
  </acceptance_criteria>
  <done>All Docker images pinned to specific versions. Python version standardized to 3.12-slim across all Dockerfiles.</done>
</task>

</tasks>

<verification>
1. `grep "latest" docker-compose.yml` returns no matches
2. `grep "3.11" telemetry-service/Dockerfile` returns no matches
3. `docker compose config > /dev/null 2>&1 && echo "valid" || echo "invalid"` outputs "valid"
</verification>

<success_criteria>
- No `:latest` tags remain in docker-compose.yml
- telemetry-service/Dockerfile uses python:3.12-slim matching orchestrator/Dockerfile
- docker compose config validates successfully
</success_criteria>

<output>
After completion, create `.planning/phases/01-housekeeping/01-02-SUMMARY.md`
</output>
