---
phase: 05-web-server-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/tests/__init__.py
  - web/tests/conftest.py
  - web/pyproject.toml
  - web/tests/test_rest.py
autonomous: true
requirements:
  - WTST-04
  - WTST-05
  - WTST-07

must_haves:
  truths:
    - "Transcription endpoint returns text and confidence from mocked WhisperClient"
    - "TTS phrase cache returns pre-generated audio without calling tts_client"
    - "TTS endpoint returns audio from mocked ElevenLabs via tts_client"
    - "Status endpoint reports subsystem health from mocked AppState"
    - "All tests pass with no external services running"
  artifacts:
    - path: "web/tests/__init__.py"
      provides: "Package marker"
    - path: "web/tests/conftest.py"
      provides: "Mock AppState fixture, test_app fixture, dependency overrides"
      contains: "dependency_overrides"
    - path: "web/pyproject.toml"
      provides: "pytest config with asyncio_mode, integration marker, and dev dependencies"
      contains: "asyncio_mode"
    - path: "web/tests/test_rest.py"
      provides: "REST endpoint tests for status, transcribe, TTS, cache"
      min_lines: 80
  key_links:
    - from: "web/tests/conftest.py"
      to: "web/server.py"
      via: "dependency_overrides[get_app_state]"
      pattern: "dependency_overrides"
    - from: "web/tests/test_rest.py"
      to: "web/tests/conftest.py"
      via: "mock_app_state and test_app fixtures"
      pattern: "test_app"
---

<objective>
Create the web server test infrastructure (conftest, pytest config, dev dependencies) and implement all REST endpoint tests covering transcription, TTS phrase cache, and status health.

Purpose: Establishes the test foundation that all subsequent test plans depend on, and covers the three REST-testable requirements (WTST-04, WTST-05, WTST-07) which are simpler than WebSocket tests.
Output: Working conftest.py with mock AppState, pyproject.toml with pytest config and dev deps, and passing REST tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-web-server-tests/05-CONTEXT.md
@.planning/phases/05-web-server-tests/05-RESEARCH.md

@web/server.py
@orchestrator/tests/conftest.py
</context>

<interfaces>
<!-- Key types and contracts from web/server.py that tests must interact with -->

From web/server.py:
```python
@dataclass
class AppState:
    settings: Any
    sim_client: TelemetryClient | None = None
    claude_client: ClaudeClient | None = None
    context_store: ContextStore | None = None
    phase_detector: FlightPhaseDetector | None = None
    whisper_client: WhisperClient | None = None
    tts_client: httpx.AsyncClient | None = None
    tts_cache: dict[str, bytes] = field(default_factory=dict)
    sim_connected: bool = False
    bridge_last_seen: float = 0.0
    bridge_connected: bool = False

def get_app_state(request: Request) -> AppState:
def get_ws_app_state(ws: WebSocket) -> AppState:

class TTSRequest(BaseModel):
    text: str
```

REST endpoints to test:
- `GET /api/status` -> JSON with sim_connected, chromadb_available, whisper_available, etc.
- `POST /api/transcribe` -> accepts UploadFile, returns {text, confidence}
- `POST /api/tts` -> accepts TTSRequest JSON, returns audio/mpeg or cache hit

Key constant: `_LOW_CONFIDENCE_THRESHOLD = 0.4`

Module-level: `settings = load_settings()` runs at import time -- tests must set ANTHROPIC_API_KEY env var before import.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create test infrastructure (conftest, pyproject, package init)</name>
  <files>web/tests/__init__.py, web/tests/conftest.py, web/pyproject.toml</files>
  <read_first>
    - web/server.py (AppState dataclass, get_app_state, get_ws_app_state signatures)
    - orchestrator/tests/conftest.py (existing project conftest pattern)
    - orchestrator/pyproject.toml (pytest config pattern with asyncio_mode and markers)
  </read_first>
  <action>
    1. Create `web/tests/__init__.py` as empty package marker.

    2. Create `web/pyproject.toml` with:
       - A `[project]` section with `name = "web"`, `version = "0.1.0"`, `requires-python = ">=3.11"`
       - A `[project.optional-dependencies]` section per D-03:
         ```toml
         [project.optional-dependencies]
         dev = [
             "httpx-ws>=0.9.0",
             "pytest>=8.0",
             "pytest-asyncio>=0.24",
         ]
         ```
       - `[tool.pytest.ini_options]` section with `asyncio_mode = "auto"`, `testpaths = ["tests"]`
       - `markers = ["integration: marks tests requiring Docker services (deselect with '-m not integration')"]`
       - `filterwarnings` to suppress DeprecationWarning noise if needed
       - Per D-04, tests runnable via `pytest web/tests/` independently

    3. Create `web/tests/conftest.py` per D-08 and D-09:
       - Import `app`, `get_app_state`, `get_ws_app_state` from `web.server`
       - Handle Pitfall 3 (module-level Settings import): use `monkeypatch` or set `ANTHROPIC_API_KEY=test-key` env var via a session-scoped autouse fixture that runs before any server import. Use `os.environ` in a conftest-level fixture with `autouse=True, scope="session"` to set the env var before the module is imported.
       - `mock_app_state` fixture: Create `AppState` with:
         - `settings`: MagicMock with explicit attributes (elevenlabs_api_key="test-key", voice_id="test-voice", elevenlabs_model_id="eleven_multilingual_v2", claude_model="claude-sonnet-4-20250514", telemetry_service_url="ws://localhost:8080/ws/telemetry", whisper_url="http://localhost:9090", chromadb_url="http://localhost:8000", log_level="DEBUG")
         - `whisper_client`: AsyncMock with `transcribe_with_confidence` and `is_available` mocked
         - `claude_client`: AsyncMock
         - `tts_client`: AsyncMock (mock for httpx.AsyncClient.post)
         - `sim_client`: MagicMock with `state` attribute
         - `context_store`: MagicMock with `available=True`, `document_count=42`
         - `tts_cache`: empty dict (tests populate as needed)
       - `test_app` fixture per D-09: Set `app.dependency_overrides[get_app_state] = lambda: mock_app_state` and same for `get_ws_app_state`. Yield `app`. Clear `app.dependency_overrides` in teardown.

    Per D-02 and D-03: httpx-ws is declared as a dev dependency in `web/pyproject.toml` under `[project.optional-dependencies]`. Do NOT modify `web/requirements.txt` (that is for runtime deps). Install test deps via `pip install -e ".[dev]"` from the `web/` directory.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/bould/source/airdale && python -c "import web.tests" && echo "package OK" && grep -q "dependency_overrides" web/tests/conftest.py && echo "conftest overrides OK" && grep -q "httpx-ws" web/pyproject.toml && echo "dev deps OK"</automated>
  </verify>
  <acceptance_criteria>
    - web/tests/__init__.py exists
    - web/tests/conftest.py contains "dependency_overrides"
    - web/tests/conftest.py contains "mock_app_state"
    - web/tests/conftest.py contains "get_app_state"
    - web/tests/conftest.py contains "get_ws_app_state"
    - web/pyproject.toml contains "asyncio_mode"
    - web/pyproject.toml contains "integration"
    - web/pyproject.toml contains [project.optional-dependencies] with httpx-ws, pytest, pytest-asyncio
  </acceptance_criteria>
  <done>conftest.py creates a fully mocked AppState and overrides both DI functions. pyproject.toml configures pytest with async mode, integration marker, and dev dependencies (httpx-ws, pytest, pytest-asyncio). Package is importable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: REST endpoint tests (status, transcribe, TTS, cache)</name>
  <files>web/tests/test_rest.py</files>
  <read_first>
    - web/server.py (get_status, transcribe_audio, text_to_speech endpoints)
    - web/tests/conftest.py (just created in Task 1)
  </read_first>
  <behavior>
    WTST-07 (status endpoint):
    - test_status_returns_subsystem_health: GET /api/status returns 200 with correct fields (sim_connected, chromadb_available, whisper_available, elevenlabs_configured, claude_model)
    - test_status_whisper_unavailable: When whisper_client.is_available returns False, whisper_available is False

    WTST-04 (transcription):
    - test_transcribe_returns_text_and_confidence: POST /api/transcribe with multipart wav file returns {text, confidence} from mocked whisper_client
    - test_transcribe_low_confidence_flag: When confidence < 0.4, response includes low_confidence=True
    - test_transcribe_webm_direct: POST with webm content type transcribes directly

    WTST-05 (TTS phrase cache):
    - test_tts_cache_hit: Pre-populate tts_cache with "Roger." -> fake MP3 bytes, POST /api/tts with {"text": "Roger."} returns cached audio without calling tts_client.post
    - test_tts_cache_miss_calls_client: POST /api/tts with uncached text calls tts_client.post and returns the response
    - test_tts_not_configured_returns_503: When elevenlabs_api_key is empty, returns 503
  </behavior>
  <action>
    Create `web/tests/test_rest.py` with all tests above using httpx.AsyncClient + ASGITransport per D-02.

    Pattern for each test:
    ```python
    async def test_example(test_app, mock_app_state):
        transport = ASGITransport(app=test_app)
        async with httpx.AsyncClient(transport=transport, base_url="http://test") as client:
            resp = await client.get("/api/status")
        assert resp.status_code == 200
    ```

    For transcription tests: Use `files={"file": ("audio.wav", b"RIFF" + b"\x00" * 100, "audio/wav")}` as fake audio.
    For TTS cache test: Pre-populate `mock_app_state.tts_cache["Roger."] = b"\xff\xfb\x90\x00" * 50` before the request. Note: the server calls `preprocess_for_tts(request.text)` before checking cache, so the cache key must match the preprocessed output. "Roger." preprocesses to "Roger." (no change), so this works directly.
    For TTS miss test: Mock `mock_app_state.tts_client.post` to return a mock response with `status_code=200`, `content=b"fake-mp3"`, and a working `raise_for_status` method.

    Important: Use `pytest.mark.asyncio` on all tests (or rely on asyncio_mode=auto from pyproject.toml).
  </action>
  <verify>
    <automated>cd /mnt/c/Users/bould/source/airdale/web && python -m pytest tests/test_rest.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - web/tests/test_rest.py contains "test_status_returns_subsystem_health"
    - web/tests/test_rest.py contains "test_transcribe_returns_text_and_confidence"
    - web/tests/test_rest.py contains "test_tts_cache_hit"
    - web/tests/test_rest.py contains "test_tts_not_configured_returns_503"
    - web/tests/test_rest.py contains "ASGITransport"
    - All tests in test_rest.py pass
  </acceptance_criteria>
  <done>8 REST endpoint tests pass covering status health (WTST-07), transcription with confidence (WTST-04), and TTS phrase cache hits/misses (WTST-05). All use mocked AppState with no external services.</done>
</task>

</tasks>

<verification>
```bash
cd /mnt/c/Users/bould/source/airdale/web && python -m pytest tests/test_rest.py -v --tb=short
```
All tests pass. No external services required.
</verification>

<success_criteria>
- conftest.py with mock AppState and dependency overrides exists and is importable
- pyproject.toml configures pytest with asyncio_mode=auto, integration marker, and dev deps
- 8+ REST tests pass covering WTST-04, WTST-05, WTST-07
- Tests run in under 10 seconds
- No Docker, Whisper, ElevenLabs, or ChromaDB required
</success_criteria>

<output>
After completion, create `.planning/phases/05-web-server-tests/05-01-SUMMARY.md`
</output>
