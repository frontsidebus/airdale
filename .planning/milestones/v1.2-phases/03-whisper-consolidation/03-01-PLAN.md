---
phase: 03-whisper-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - orchestrator/orchestrator/whisper_client.py
  - orchestrator/tests/test_whisper_client.py
autonomous: true
requirements:
  - WHSP-01
  - WHSP-02
  - WHSP-03

must_haves:
  truths:
    - "A single async WhisperClient exists with persistent httpx.AsyncClient"
    - "Confidence scoring uses exp(avg_logprob) from verbose_json segments, falling back to 0.5"
    - "Failed requests retry 3 times with exponential backoff; 4xx errors do not retry"
    - "Client exposes transcribe() and transcribe_with_confidence() async methods"
    - "Client has aclose() for lifecycle management following TTS pattern"
  artifacts:
    - path: "orchestrator/orchestrator/whisper_client.py"
      provides: "Async WhisperClient with retry, confidence, aclose"
      exports: ["WhisperClient", "TranscriptionResult", "WhisperClientError"]
    - path: "orchestrator/tests/test_whisper_client.py"
      provides: "Async tests for the unified WhisperClient"
      min_lines: 200
  key_links:
    - from: "orchestrator/orchestrator/whisper_client.py"
      to: "/v1/audio/transcriptions"
      via: "httpx.AsyncClient.post"
      pattern: "v1/audio/transcriptions"
    - from: "orchestrator/orchestrator/whisper_client.py"
      to: "orchestrator/orchestrator/audio_processing.py"
      via: "AVIATION_PROMPT import"
      pattern: "from .audio_processing import AVIATION_PROMPT"
---

<objective>
Replace the existing sync WhisperClient with an async version using persistent httpx.AsyncClient, unified confidence scoring, and retry logic with exponential backoff. This is the foundation -- Plan 02 wires it into consumers.

Purpose: Single source of truth for Whisper transcription logic (WHSP-01), unified confidence scoring (WHSP-02), and retry logic (WHSP-03).
Output: Async WhisperClient class + comprehensive test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-whisper-consolidation/03-CONTEXT.md

@orchestrator/orchestrator/whisper_client.py
@orchestrator/orchestrator/audio_processing.py
@orchestrator/orchestrator/config.py
@orchestrator/tests/test_whisper_client.py
@orchestrator/orchestrator/tts/base.py

<interfaces>
<!-- Existing sync WhisperClient being replaced (from whisper_client.py) -->
```python
class TranscriptionResult:
    text: str
    confidence: float  # 0.0 to 1.0
    language: str
    duration_secs: float

class WhisperClientError(Exception): ...

class WhisperClient:
    def __init__(self, base_url, timeout, model, language, initial_prompt): ...
    def transcribe(self, audio_bytes, *, output_format, language) -> str: ...
    def transcribe_with_confidence(self, audio_bytes, *, language) -> TranscriptionResult: ...
    def is_available(self) -> bool: ...
    def close(self) -> None: ...
```

<!-- TTS pattern to follow (from tts/base.py) -->
```python
class TTSClient(Protocol):
    async def synthesize(self, text: str) -> bytes: ...
    async def aclose(self) -> None: ...
```

<!-- Config fields (from config.py) -->
```python
class Settings:
    whisper_model: str = "medium"
    whisper_url: str = "http://localhost:9090"
```

<!-- Aviation prompt (from audio_processing.py) -->
```python
AVIATION_PROMPT: str  # Multi-line string with ATIS, METAR, squawk, NATO phonetic, etc.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite WhisperClient as async with persistent httpx.AsyncClient</name>
  <files>orchestrator/orchestrator/whisper_client.py, orchestrator/tests/test_whisper_client.py</files>
  <read_first>
    - orchestrator/orchestrator/whisper_client.py
    - orchestrator/orchestrator/audio_processing.py
    - orchestrator/orchestrator/config.py
    - orchestrator/orchestrator/tts/base.py
    - orchestrator/tests/test_whisper_client.py
  </read_first>
  <behavior>
    - Test: WhisperClient constructor sets base_url (strips trailing slash), timeout, model, language, initial_prompt (defaults to AVIATION_PROMPT)
    - Test: transcribe() sends POST to /v1/audio/transcriptions with model, language, response_format=text, prompt, file upload; returns stripped text
    - Test: transcribe_with_confidence() sends verbose_json format, returns TranscriptionResult with confidence=exp(avg_logprob) averaged across segments
    - Test: Confidence defaults to 0.5 when no segments returned
    - Test: Low avg_logprob (-1.5) produces confidence < 0.3
    - Test: Retry on ConnectError — recovers after 2 failures on 3rd attempt
    - Test: Raises WhisperClientError after 3 failed retries
    - Test: Backoff timing is 1.5*attempt seconds (uses asyncio.sleep, not time.sleep)
    - Test: No retry on 4xx errors (400, 422) — raises immediately
    - Test: Retries on 5xx errors
    - Test: Retries on timeout errors
    - Test: is_available() returns True on 200, False on error/non-200
    - Test: aclose() closes the underlying httpx.AsyncClient
    - Test: async context manager (__aenter__/__aexit__) works
  </behavior>
  <action>
    Rewrite orchestrator/orchestrator/whisper_client.py to be fully async (per D-01, D-02, D-03, D-04):

    1. Replace `httpx.Client` with `httpx.AsyncClient` (persistent, created in __init__).
    2. Make `transcribe()` and `transcribe_with_confidence()` async methods using `await self._client.post(...)`.
    3. Replace `time.sleep()` with `await asyncio.sleep()` in retry loops.
    4. Use `/v1/audio/transcriptions` endpoint exclusively (per D-02). Drop any `/asr` references.
    5. Keep the exact same confidence scoring logic (per D-05): request `verbose_json`, extract `avg_logprob` from segments, compute `confidence = min(1.0, max(0.0, math.exp(avg_logprob)))`, fallback to 0.5 when no segments.
    6. Keep both `transcribe()` (text only) and `transcribe_with_confidence()` (text + score) methods (per D-06).
    7. Keep retry logic: 3 attempts, exponential backoff 1.5*attempt, no retry on 4xx (per D-07).
    8. Add `async def aclose()` method that calls `await self._client.aclose()` (per D-04).
    9. Add `async def is_available()` method using async GET to /health.
    10. Implement `__aenter__`/`__aexit__` for async context manager support.
    11. Keep `TranscriptionResult` dataclass, `WhisperClientError`, and all module-level constants unchanged.

    Then rewrite orchestrator/tests/test_whisper_client.py:
    - Convert all test classes to use `pytest.mark.asyncio` and async test methods.
    - Mock `httpx.AsyncClient` instead of `httpx.Client`.
    - Patch `orchestrator.whisper_client.asyncio.sleep` instead of `time.sleep`.
    - Keep the same test structure and coverage: constructor, transcribe success, confidence scoring, retry on connection error, no retry on 4xx, retry on 5xx, timeout handling, health check, context manager.
    - Use `AsyncMock` for the httpx client mock where needed.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/bould/source/airdale/orchestrator && python -m pytest tests/test_whisper_client.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "async def transcribe" orchestrator/orchestrator/whisper_client.py
    - grep -q "async def transcribe_with_confidence" orchestrator/orchestrator/whisper_client.py
    - grep -q "async def aclose" orchestrator/orchestrator/whisper_client.py
    - grep -q "httpx.AsyncClient" orchestrator/orchestrator/whisper_client.py
    - grep -q "asyncio.sleep" orchestrator/orchestrator/whisper_client.py
    - ! grep -q "time.sleep" orchestrator/orchestrator/whisper_client.py
    - ! grep -q "httpx.Client(" orchestrator/orchestrator/whisper_client.py (no sync client)
    - grep -q "v1/audio/transcriptions" orchestrator/orchestrator/whisper_client.py
    - ! grep -q "/asr" orchestrator/orchestrator/whisper_client.py
    - grep -q "math.exp" orchestrator/orchestrator/whisper_client.py (confidence scoring preserved)
    - grep -q "pytest.mark.asyncio" orchestrator/tests/test_whisper_client.py
    - All tests in test_whisper_client.py pass
  </acceptance_criteria>
  <done>
    Async WhisperClient exists with: persistent httpx.AsyncClient, transcribe() returning text, transcribe_with_confidence() returning TranscriptionResult with exp(avg_logprob) confidence, 3-retry exponential backoff (no retry on 4xx), aclose() lifecycle, async context manager. All tests pass.
  </done>
</task>

</tasks>

<verification>
- `cd orchestrator && python -m pytest tests/test_whisper_client.py -x -v` — all tests pass
- `grep -c "async def" orchestrator/orchestrator/whisper_client.py` — at least 5 async methods (transcribe, transcribe_with_confidence, is_available, aclose, __aenter__, __aexit__)
- `grep "httpx.Client(" orchestrator/orchestrator/whisper_client.py` — no sync client references
- `python -c "from orchestrator.whisper_client import WhisperClient, TranscriptionResult, WhisperClientError"` — imports work
</verification>

<success_criteria>
A single async WhisperClient class in whisper_client.py with persistent httpx.AsyncClient, unified confidence scoring (exp(avg_logprob)), retry with backoff, and aclose(). All tests pass. No sync httpx.Client remains.
</success_criteria>

<output>
After completion, create `.planning/phases/03-whisper-consolidation/03-01-SUMMARY.md`
</output>
